# The public ADSS host, without a trailing slash.
ADSS_BASE_URL=https://ai-scope.cbpf.br
# Comma-separated development origins. In production, serve the built UI from
# FastAPI and leave this unset.
CORS_ORIGINS=http://localhost:5173
# Set to true behind HTTPS. Keep false only for local development.
COOKIE_SECURE=false
